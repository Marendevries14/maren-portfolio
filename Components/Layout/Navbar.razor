@using MVDesign.Components.UI

<header class="fixed inset-x-0 top-0 z-50 border-b border-cocoa/10 bg-paper/85 backdrop-blur-xl">
    <Container>
        <nav class="flex h-24 items-center justify-between">
            <a href="/" class="flex items-center gap-4">
                <span class="text-5xl font-display text-primary">M</span>
                <span class="hidden leading-tight sm:block">
                    <span class="block font-semibold text-cocoa">Maren de Vries</span>
                    <span class="text-sm text-cocoa/60">Software Engineer · Project Management</span>
                </span>
            </a>

            <div class="hidden items-center gap-10 text-sm text-cocoa/70 md:flex">
                <a href="#over" class="hover:text-primary">Over mij</a>
                <a href="#expertise" class="hover:text-primary">Expertise</a>
                <a href="#projecten" class="hover:text-primary">Projecten</a>
                <a href="#contact" class="hover:text-primary">Contact</a>
            </div>

            <a href="#contact" class="rounded-full bg-primary px-6 py-3 text-sm font-semibold text-white soft-shadow">
                Laten we praten
            </a>
        </nav>
    </Container>
</header>